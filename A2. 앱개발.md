# 지침

1. 앱 버전 관리 규칙 : v{Major}.{Minor}.{Patch}
   - Major : 사용자 선택 사항.
   - Minor : "A1. 개발-프로세스.md" 의 phase 별 버전 규칙을 따름.
   - Patch : 버그 수정. 기존 기능 개선 또는 수정. 개발 phase가 변경되지 않는 수정 사항들 모두.

그외, 애매한 부분에 대해서는 사용자에게 버전 변경을 물어본 뒤 그 내용을 기록한다.
앱 버전을 앱 내의 global 상수로 기록하고 조회, 표시 될 수 있도록 한다.

코드 작성 중 {Minor} 변경 사항에 대해, 모듈/블록 추가에 대해서는 앱 버전을 comment로 기록해둔다.
코드 작성 중 {Patch} 변경 사항에 대해, 수정 내용 부분에 앱 버전을 comment로 기록해둔다.

2. 섹션 : "앱 개발 내용" - 앱 전체 개발에 대한 구체적인 계획 및 내용을 기록한다. (실제 앱 설계서)

3. 섹션 : "앱 버전별/날짜별 개발, 추가, 수정 등의 작업 History"
   - 실제 개발된 내용을 기록한다. 직접 코드를 여기에 작성하는 일은 최소화 하되, 꼭 필요하다면 추가한다.
   - 모듈별 또는 Phase별로 지난 내용에 대해서는 모두 "앱개발-아카이브.md"로 이관/정리한다.

4. 섹션 : "버그" - 자체적으로 파악한 버그 및 사용자가 보고한 버그 사항을 표로 정리하여 모두 기록한다. 앱 버전과 보고된 날짜를 기록하고 이후 처리 상황에 따라 표를 정리한다.

5. 섹션 : "다음 작업 할 내용 정리" - 위 "앱 버전별/날짜별 개발, 추가, 수정 등의 작업 History" 이후에 진행해야할 작업 내용을 미리 기록해 둔다. 필요하다면 승인 받아야 할 내용도 함께 기록해서 이후 바로 진행 될 수 있도록 내용을 정리한다.

---

# 앱 개발 내용 (앱 설계서)

## 0. 제품 정의

| 항목 | 내용 |
|------|------|
| 제품명(가칭) | FocusDAW — Mastering Desk |
| 목적 | 오디오 파일에 7단계 마스터링 체인 적용 → 실시간 Preview + 일괄 Export |
| 플랫폼 | Electron 단독 실행(Windows 우선), borderless 윈도우 |
| 스택 | Vite + React + TypeScript + Tailwind + Zustand / Web Audio API / electron-builder |
| 디자인 소스 | `Mastering Desk Studio.dc.html` (실제 수정 대상), `standalone.html`(참고) |
| 버전 상수 | `src/version.ts` → `export const APP_VERSION = 'x.y.z'` (타이틀바·About 표시) |

## 1. 폴더 구조 (계획)

```
src/
  audio/      디코딩·파일픽커·플레이어·STFT (FocusSpectogram 이식)
  dsp/        체인 노드: pre, eq, dynamics, stereo, loudness, limiter.worklet
  export/     wav / mp3(lamejs) / flac(libflac) 인코더 + 태그(ID3/Vorbis)
  ui/         desk 7섹션 패널 + 공통 Knob·Meter·Graph 컴포넌트
  store/      appStore (masterChain 파라미터·큐·재생상태)
  theme/      8테마 정의 + applyTheme(CSS 변수)
  version.ts
electron/     main.cjs(borderless·autoUpdater) · preload.cjs
```

## 2. 시그널 체인 설계 (7단계)

신호: SOURCE → Input → Pre → Spectral EQ → Dynamics → Stereo → Loudness/Limiter → Export(MASTER). 좌→우 직렬. 각 단계 **Bypass = dry pass-through**. 모든 주파수 상한 = `min(설정값, Nyquist=SR/2)`.

| # | 단계 | 핵심 컨트롤 | DSP 구현 |
|---|------|------------|----------|
| I | Input | Source·PCM·Rate·Recursive·Normalize | decodeAudioData → PCM float, Nyquist 산출 |
| II | Pre | Denoise·Noise Depth(1/2/3)·Fade In/Out | STFT 스펙트럴 게이팅(FocusSpectogram) + 3D 워터폴 |
| III | Spectral EQ | **Min-φ 5밴드 Parametric / 9-Band Graphic** | BiquadFilter 기반. Linear/Dynamic 모드 범위 제외 |
| IV | Dynamics | Low/Mid/High·Ratio·Transient·Exciter | LR 3밴드 크로스오버 + DynamicsCompressor + 셰이퍼/익사이터 |
| V | Stereo | Width·Reverb·Delay·Bass Mono·Mono Compat | M/S 처리, 저역 크로스오버 모노, 상관도 측정(상시) |
| VI | Loudness | True Peak·LUFS·Saturate·Limiter·TP Limit | BS.1770-4 LUFS, 4× 오버샘플 TP, 룩어헤드 리미터 Worklet |
| VII | Export | 메타·아트워크·Format·Destination | OfflineAudioContext 렌더 → 인코딩 → 태그 |

### Sampling Rate / Buffer 정책 (확정 방향)

Input의 `Rate(44.1k / 48k / 96k)`는 **내부 처리 샘플레이트**로 정의한다. 원본 파일의 샘플레이트와 PC H/W 출력 장치 샘플레이트가 달라도, DSP와 Export 결과가 사용자 설정 Rate 기준으로 일관되도록 다음 구조를 사용한다.

#### 1) 버퍼 역할 분리

| 버퍼 | 샘플레이트 | 용도 | 변경 시점 |
|------|------------|------|-----------|
| `sourceBuffer` | 원본 파일 샘플레이트 보존 | 좌측 원본 Play, 재처리 기준 원본 | 파일 로딩 시 생성 후 유지 |
| `processingBuffer` | 사용자 설정 Rate | 중앙 Preview, II~VI DSP, Batch Export | Rate 변경 또는 최초 Preview/Export 시 생성/재생성 |

예: 원본 44.1kHz 파일을 불러오고 사용자가 Input Rate를 48kHz로 설정한 경우:

```text
sourceBuffer.sampleRate = 44100
processingBuffer.sampleRate = 48000

좌측 원본 Play:
sourceBuffer → AudioContext.destination

중앙 Preview:
processingBuffer → Pre/EQ/Dynamics/Stereo/Loudness chain → AudioContext.destination

Batch Export:
processingBuffer 또는 sourceBuffer→OfflineAudioContext(48000) 렌더 → 48kHz 파일 출력
```

#### 2) PC H/W 샘플레이트와의 관계

PC 오디오 장치/OS/`AudioContext.sampleRate`는 사용자마다 44.1kHz, 48kHz, 96kHz 등으로 다를 수 있다. 이 값은 **최종 스피커 출력 레이트**일 뿐, 앱의 DSP 기준으로 사용하지 않는다.

- DSP 기준 샘플레이트: `processingBuffer.sampleRate`
- EQ Nyquist, STFT bin, limiter/oversampling 기준: 사용자 설정 Rate / `processingBuffer.sampleRate`
- 최종 스피커 출력 변환: Web Audio가 `AudioContext.destination`에서 장치 레이트에 맞춰 처리

따라서 H/W sample rate가 달라도, 같은 입력·같은 설정이면 마스터링 처리 기준은 동일해야 한다. 구현 시 `AudioContext.sampleRate`를 DSP 기준으로 직접 참조하지 않도록 주의한다.

#### 3) 파일 로딩 후 Rate 변경 대책

사용자가 파일을 불러온 뒤 Input Rate를 바꾸는 경우:

1. `sourceBuffer`는 원본 보존용이므로 변경하지 않는다.
2. 현재 재생 중인 원본/Preview를 정지한다.
3. 기존 `processingBuffer`와 `processingSampleRate`를 무효화한다.
4. UI의 Rate chip, EQ Nyquist, 그래프 주파수 상한은 즉시 새 Rate 기준으로 갱신한다.
5. 다음 Preview 또는 Export 시 `sourceBuffer`를 새 사용자 Rate로 리샘플링해 `processingBuffer`를 다시 만든다.

권장 구현은 **Lazy 변환**이다. Rate 변경 즉시 큐 전체를 변환하지 않고, 필요한 파일을 Preview/Export 할 때 `processingBuffer`가 없거나 `processingSampleRate !== userRate`이면 그때 변환한다. 긴 파일/다중 파일 큐에서 불필요한 대기 시간을 줄이기 위함이다.

```ts
type QueueFile = {
  sourceBuffer: AudioBuffer;        // 원본 보존
  processingBuffer?: AudioBuffer;   // 사용자 Rate 내부 처리용
  processingSampleRate?: number;
  meta: AudioMeta;
}
```

> v0.2.2에서 `QueueFile`은 `sourceBuffer`/`processingBuffer` 구조로 전환되었다. `processingBuffer`는 Preview 실행 시 사용자 Input Rate 기준으로 lazy 생성된다.

### 공통 노브 동작 규칙
- 세로 드래그(위=증가, 감도 ≈ 범위/140px), 휠 1노치=1스텝(정수 ±1·소수 ±0.1·주파수 ±5Hz), 더블클릭=기본값 리셋.
- 상위 토글 off → 종속 노브 회색+입력 차단(Bass Mono off→Bass, TP Limit off→True Peak 등).
- LED/arc 링 `-135°~+135°`(270°) 값 비례.

### 처리/측정 세부 (요청서·_참고 반영)
- **Noise Depth**: 1=Original(최소)·2=Normal·3=Deep. 기존 앱(FocusSpectogram) 노이즈 제거 방식 사용.
  - **음악 MP3 음원별 지능형 Denoise 설정 추천 기준**:
    | SNR 범위 | 판정 상태 | 추천 Depth | 추천 노브량 | 표시 색상 |
    | :--- | :--- | :---: | :---: | :---: |
    | 100 dB 이상 | Very Clean | Original (1) | 5% | 파란색 (`#4ea5ff`) |
    | 90 ~ 100 dB | Clean | Original (1) | 10% | 파란색 (`#4ea5ff`) |
    | 80 ~ 90 dB | Light Clean | Original (1) | 25% | 초록색 (`#46d36e`) |
    | 60 ~ 80 dB | Moderate Noise | Normal (2) | 10% | 연두색 (`#a2db34`) |
    | 40 ~ 60 dB | Heavy Noise | Normal (2) | 30% | 오렌지색 (`#ff983d`) |
    | 40 dB 미만 | Extreme Noise | Deep (3) | 50% | 붉은색 (`#ff5a5a`) |
- **Correlation 미터**: 측정값 → 토글과 무관하게 항상 표시(≥0.5 GOOD/0~0.5 CHECK/<0 RISK), fold loss(dB) 상시.
- **Saturation THD 판정**: THD% + 짝수/홀수 배음비 + 크레스트 팩터 감소 + 고역 누적/TP 상승 + 모노 청감 → GENTLE/MUSICAL/HOT, Saturate arc·숫자·전달곡선·배음 막대 전체 연동.
- **LUFS 미터 색**: 절대레벨 기준 −14↓파랑 → −9 초록 → −7 노랑 → 빨강(그라데이션 블렌딩).

## 3. 테마 시스템
- 8테마: Teal / Sunset / Violet / Crimson + 각 Light(명도 반전). 표준 팔레트 키(`_refer/variations.html` 방식) → CSS 변수.
- 액센트(메인/밝은/글로우/잉크) + 배경(데스크/시트/패널/카드) 일관 적용. 경고색(빨강/노랑)은 테마 무관 고정.

## 4. 빌드·배포·자동 업데이트
- electron-builder NSIS(Windows), `publish: github` → 저장소 `limsunglyong/FocusDAW-Mastering` Releases.
- electron-updater: 기동 시 업데이트 확인 → 사용자 동의 시 다운로드 → `Restart now` 선택 시 적용.
- 타이틀바 칩: Input Rate·Bit, Export Format 실시간 표시.
- **코드 서명**: 초기 미서명 빌드. 미서명 시 Windows SmartScreen 경고가 뜰 수 있으나 빌드/업데이트 구조는 동일 → 추후 OV 인증서 발급 시 `electron-builder` 서명 설정만 추가(구조 변경 없음).

## 4-1. 확정 사항 (2026-06-26)
- GitHub 저장소: `https://github.com/limsunglyong/FocusDAW-Mastering.git`
- 인코더: MP3 = lamejs, FLAC = libflac.js
- 코드 서명: 미서명으로 시작(추후 OV 인증서 확장 가능)

---

# 앱 개발 계획 (Phase 별)

> 상세 일정·도식은 `A1. 개발-프로세스.md` 참조. 각 Phase 완료 시 본 문서 History와 `A3. 시험.md`에 기록.

| Phase | 버전 | 내용 | 상태 |
|-------|------|------|------|
| 0 | v0.1.x | 셋업·UI 셸·테마 골격·버전 상수 | **완료 — 현재 v0.1.1 (원본 UI 충실 이식 포함)** |
| 1 | v0.2.0 | Input·큐·엔진 코어·Preview·공통 노브 | **완료** — 실제 파일 입력·큐·디코딩·메타·원본 LUFS·Preview 재생 연결 완료 |
| 2 | (v0.2.24~33) | Pre Processing(디노이즈·페이드·3D 워터폴) | **완료** — STFT 스펙트럴 디노이즈, 3D 워터폴 서피스, SNR 적응형 추천 탑재 완료. *(계획상 v0.3.0 Minor 승격은 생략하고 v0.2.x 패치로 완료 — 버전 정합성 메모 참조)* |
| 3 | v0.4.x | Spectral EQ(5밴드·Min-φ·그래프·프리셋) | **기존 Phase 완료** — 5밴드 Biquad(Min-φ), 그래프 노드 드래그(Freq/Gain)·휠(Q±0.1), SR 연동 주파수축, 프리셋 5종·User 저장·Advanced 패널. 이후 추가된 9-Band Graphic 옵션도 사용자 회귀 시험 PASS, 버전 반영만 대기. Linear/Dynamic 모드는 범위 제외. |
| 4 | v0.5.0 | Dynamics(멀티밴드 컴프·트랜지언트·익사이터) | **완료(Preview)** — LR 3밴드 크로스오버·대역별 컴프·트랜지언트(어택/릴리즈)·익사이터(고역 하모닉) 구현. *(트랜지언트 정밀 셰이퍼·Export 정밀화는 후속)* |
| 5 | v0.6.0 | Stereo(Width·Bass Mono·상관도) | **완료(Preview)** — Width(M/S)·Bass Mono·Reverb/Delay send·Mono Compat·실측 Correlation 미터(상시) 구현. *(Export 정밀화는 후속)* |
| 6 | v0.7.0 | Loudness/Limiter(LUFS·TP·새츄레이션) | **완료(Preview)** — LUFS make-up 게인·Saturation·**룩어헤드 TP 리미터(AudioWorklet)**·캐릭터별 릴리즈·THD 판정. *(정밀 폴리페이즈 ISP TP·Export 정밀화는 후속)* |
| 7 | v0.8.0 | Export·Batch 렌더(인코딩·태그) | **진행 중** — 마스터 체인 빌더 분리(7-A)·오프라인 정밀 렌더(7-B)·**WAV(RIFF) 인코더(7-C)**·Electron 파일 저장 IO(7-D)·Export UI(단일/배치·진행률·취소·아트워크·Destination, 7-F/7-G)·**MP3 320(ID3v2+APIC)·FLAC(VORBIS_COMMENT+PICTURE) 인코더(7-E, v0.8.4)** 까지 **WAV/MP3/FLAC end-to-end 동작**. *(Preview↔Export 정합 시험(7-H)·MP3/FLAC 사용자 청취 확인은 후속.)* |
| 8 | v0.9.0 | 테마 완성·borderless·폴리시 | 예정 |
| 9 | v0.10.0 | GitHub 자동 업데이트·패키징 | 예정 |
| 10 | v1.0.0 | 최종 인수 시험·릴리스 | 예정 |

---

# 앱 버전별/날짜별 개발, 추가, 수정 등의 작업 History

| 버전 | 날짜 | 구분 | 내용 |
|------|------|------|------|
| v0.11.3 | 2026-07-01 | Patch (업데이트 UI 단일화) | **업데이트 표시 중복 제거 및 Footer 알림 추가.** ① 기존 우측 하단 `UpdateBanner`와 관련 상태를 완전히 제거해 중앙 `CheckUpdateModal`만 사용한다. 다운로드 선택 시 중앙 창을 닫지 않고 진행률→다운로드 완료→Restart now 단계까지 같은 창에서 이어진다. ② 최신/서버 오류/Release·파일 없음/dev를 모두 비경고형 **No update available** 안내로 통합하고 원본 오류 내용은 표시하지 않는다. ③ 기동 자동 확인에서 유효한 신규 버전을 발견하면 [Footer.tsx](src/ui/desk/Footer.tsx)의 현재 버전 왼쪽에 `Update vX is available` 문구가 오른쪽에서 왼쪽으로 반복 흐른다(`dkupdateticker`). 자동 확인은 중앙 창을 띄우지 않는다. **검증:** `lint`·`build`·`verify(93/93)`·main 구문 검사 및 사용자 설치본 시험 PASS. Windows x64 NSIS 정식 설치본(`FocusDAW - Mastering Desk Setup 0.11.3.exe`, 109.02 MiB)과 blockmap/`latest.yml` 생성 완료. SHA-256 `A39392FDD810B04E1F21FDCDE97DFD5E3AFDA7A0B8D9F009DBE6683AF7F626FB`. *(A3 완료 이력 `MT-UPD-UI`.)* |
| v0.11.2 | 2026-07-01 | Patch (Updater fallback) | **GitHub Release 또는 업데이트 파일이 없을 때 앱 정상 실행 보장.** updater 상태에 요청 출처(`auto`/`manual-check`/`download`)를 추가했다. 기동 시 자동 확인 실패는 상세 오류를 콘솔에만 기록하고 renderer에서는 조용히 무시하여 앱 UI와 본 기능을 전혀 가리지 않는다. 사용자가 Help에서 직접 확인하거나 다운로드한 경우에는 원본 HTML/XML 응답을 노출하지 않고 “현재 업데이트 정보 없음/현재 버전 계속 사용 가능”이라는 짧은 안내만 표시한다. **검증:** `lint`·`build`·`verify(93/93)`·Electron main/preload 구문 검사 통과. *(Release 부재 설치본 시험은 A3 `MT-UPD-FALLBACK`.)* |
| v0.11.1 | 2026-07-01 | Patch (업데이트 선택권) | **자동 업데이트 다운로드·종료 시 설치를 사용자 선택형으로 변경.** 앱 기동 시 신규 버전 확인은 유지하지만 [main.cjs](electron/main.cjs)의 `autoDownload=false`·`autoInstallOnAppQuit=false`로 변경했다. 신규 버전 발견 시 배너/수동 확인 창에 **Download update / Later**를 표시하고, 사용자가 다운로드를 선택한 경우에만 신규 `updater:download` IPC가 `downloadUpdate()`를 실행한다. 다운로드 완료 후에도 **Restart now**를 선택한 경우에만 `quitAndInstall()`하며, **Later** 또는 일반 앱 종료 시 현재 버전을 유지한다. preload/type 계약과 Release Notes를 함께 갱신했다. **검증:** `lint`·`build`·`verify(93/93)`·Electron main/preload 구문 검사 통과. *(설치본 end-to-end 선택 동작은 A3 §5 `MT-UPD-CHOICE`.)* |
| v0.11.0 | 2026-07-01 | Minor (통합 릴리스) | **후속 기능·수정 통합 버전.** OGG/M4A 메타 파서, AIFF 직접 디코딩, 9-Band Graphic EQ, 다중 Render Batch Job, Normalize 직접 Export 레이스 수정, Render Batch 활성 Job/파일 glow·breathing 및 Cancel 즉시 회색 피드백을 포함한다. 사용자 실앱 회귀 시험과 자동 검증(`lint`·`build`·`verify 93/93`) 완료. `package.json`·lock·인앱 버전·Release Notes를 v0.11.0으로 통일하고 Windows x64 NSIS 설치본(`FocusDAW - Mastering Desk Setup 0.11.0.exe`, 109.09MB) 빌드 완료. |
| v0.10.4 | 2026-06-30 | Patch (Denoise) | **II.Pre-Proc Denoise 기능 성능 극대화.** ① 시간 영역 RMS/Peak 기반 조용한 2초 구간 고속 자동 탐색 (~10ms) 구현. ② 해당 2초 구간의 모노 신호만 STFT 및 `buildNoisePrint` 연산으로 축소 (~10ms). ③ `processChannel` 루프 내 `Math.hypot`/`Math.log10` 제거 및 임계 진폭 사전 테이블 연산 적용. ④ 각 오디오 채널(L/R)을 별도 Web Worker로 병렬화 구동하여 멀티코어 처리 최적화 (2ch 2배속). **검증:** lint·build 통과. *(A3 §5 MT-10.4.)* <br>**코드 검토 결과(2026-06-30, 안전 — 변경 유지 가능):** ①·③·④는 무해. **③ `processChannel` 사전 테이블화는 수학적으로 기존과 완전 동일**(임계값 `db>profileDb+thresholdDb` ⇔ `mag>thresholdAmp[k]=(10^((profileDb+thresholdDb)/20)−1e-12)/ampScale`, dB↔진폭 단조라 부등호 보존 / 차감량 `subAmp[k]/mag`도 동일 / `hypot→sqrt`는 오디오 진폭대에서 오버플로 無로 안전). **④ 워커**: `print`는 transfer 목록 외라 구조화 복제되어 L/R 워커가 각자 사본 사용·원본 무손상, 채널 데이터도 복사본만 transfer. **print 캐싱**(`file.noisePrint`)은 지문이 depth/amt와 무관(그 값은 `depthToOptions`로 워커서 별도 적용)하므로 재사용 정당. **유의점(버그 아님)**: ① 시간 도메인 탐색의 점수식은 *공식*은 기존과 같으나 *입력 물리량*이 다름(기존=bin별 dB 로그평균 ~−100dB대, 신규=시간영역 RMS/Peak dBFS ~−60dB대). 페널티 상수(−28/+45)가 옛 스펙트럴 스케일 기준이라 **선택 구간이 미세하게 달라질 수 있음**. 다만 `avgDb`(RMS)가 선택을 지배해 가장 조용한 구간을 안정적으로 찾고, transient 페널티는 오히려 더 의미있게 동작. **권장**: 노이즈 많은 실파일로 신/구 A/B 청취 1회 확인(만족 시 그대로 유지). *(참고: [denoiseAudioBuffer](src/audio/denoise.ts)는 미사용 dead path — 활성 경로는 `denoiseBuffer`.)* |
| v0.10.3 | 2026-06-30 | Patch (A4 ④) | **About 창 연락 이메일 추가.** Help ▸ About 의 OK 버튼 위에 `focustone.el@gmail.com` 표시(윗줄·아랫줄 한 칸씩 간격, [AboutWindow.tsx](src/ui/desk/AboutWindow.tsx)). 한 줄 추가에 맞춰 About 창 높이 370→400([main.cjs](electron/main.cjs)). **검증:** lint·build·verify(90/90) 통과. *(A3 §5 MT-10.3.)* |
| v0.10.2 | 2026-06-30 | Patch (A4 ③) | **Help ▸ Check for Updates 결과 창.** 클릭 시 수동 업데이트 확인 → 결과 모달 안내(최신/업데이트 가능/다운로드 완료=Restart/오류/dev). ① **updater IPC 리팩터**([main.cjs](electron/main.cjs)) — `autoUpdater` 인스턴스·이벤트 배선을 `setupAutoUpdater` 밖으로 hoist(`getAutoUpdater`/`wireUpdaterEvents`/`sendUpdaterStatus`), `updater:check`·`updater:restart` 핸들러를 **항상 등록**(개발 모드는 `'dev'` 상태 반환 = "설치본에서만 가능"). ② **store**([appStore.ts](src/store/appStore.ts)) `updateCheck` 상태 + `checkForUpdates`/`setUpdateCheckResult`/`closeUpdateCheck`. `onStatus` 수신 시 모달이 열려 있으면 결과 반영(`progress`→`available` 매핑), 자동 배너는 가능/진행/완료/오류만 표시(기존 유지). ③ **UI** [App.tsx](src/App.tsx) `CheckUpdateModal`(ExportNotice 패턴) + [TitleBar.tsx](src/ui/desk/TitleBar.tsx) 메뉴 연결 + [global.d.ts](src/global.d.ts) `'dev'` 상태. **검증:** lint·build·verify(90/90) 통과. *(창 표시/최신·업데이트 안내는 사용자 시험 — A3 §5 MT-10.2.)* |
| v0.10.1 | 2026-06-30 | Patch (A4 ②) | **Release Notes 메뉴/창 추가.** v0.10.0 릴리스 이후 업데이트 내용 고지용. ① Help 메뉴 `Check for Updates...` 아래 `Release Notes` 항목([data.ts](src/desk/data.ts)·[TitleBar.tsx](src/ui/desk/TitleBar.tsx)). ② 새 borderless **모달 창**(`#releasenotes`, About 패턴) — [main.cjs](electron/main.cjs) `win:open-release-notes`(460×520, parent+modal), [preload.cjs](electron/preload.cjs) `win.openReleaseNotes`, [global.d.ts](src/global.d.ts) 타입, [App.tsx](src/App.tsx) 라우팅, 신규 [ReleaseNotesWindow.tsx](src/ui/desk/ReleaseNotesWindow.tsx)(로고+버전/날짜 헤더 + New Features/Improvements/Bug Fixes 섹션, 빈 섹션 숨김, OK 닫기). ③ 내용 단일 소스 신규 [releaseNotes.ts](src/releaseNotes.ts)(`RELEASE_NOTES`) — **현재 버전만** 기록(버전 올릴 때마다 교체, 이전 노트 미보존), 버그=상세/기능·개선=간략. **검증:** lint·build·verify(90/90) 통과. *(창 표시/메뉴 동작은 사용자 시험 — A3 §5 MT-10.1.)* |
| v0.10.0 | 2026-06-29 | Phase 9 (Minor) | **GitHub 자동 업데이트 연결(electron-updater).** Phase 9 핵심 갭(autoUpdater 미연결) 해소. ① **메인 프로세스**([main.cjs](electron/main.cjs)) `setupAutoUpdater()` — 패키징 모드에서만 동작(`isDev` skip), 기동 직후 `autoUpdater.checkForUpdates()` 1회, `autoDownload=true`. `checking-for-update`/`update-available`/`download-progress`/`update-downloaded`/`error` 이벤트를 `updater:status` IPC 로 메인 창에 전달. `updater:restart`(→`quitAndInstall`)·`updater:check` 수신. ② **preload**([preload.cjs](electron/preload.cjs)) `updater.onStatus/restart/check` 노출 + [global.d.ts](src/global.d.ts) 타입. ③ **인앱 배너**([App.tsx](src/App.tsx) `UpdateBanner`) — 하단 우측 테마형 카드. available=다운로드 중·progress=% 진행바·downloaded="Restart now"/"Later"·error=메시지. `not-available`/`checking` 은 배너 미표시. 결정(2026-06-29 사용자): UI=인앱 배너, 체크=기동 시 1회. **검증:** `npm run lint`·`npm run build`·`npm run verify`(90/90) 통과. *(릴리스 발행(electron:publish·GH_TOKEN)·M7 end-to-end 자동 업데이트 시험은 Phase 10 — 패키징 후 수행.)* |
| v0.9.2 | 2026-06-29 | Patch | **Render Batch·Export 보완 묶음.** ① Render Batch 우측 상단 버튼 연결(TransportBar) 및 Project ▸ Render Batch… 메뉴. ② Render Batch 표시 중 메인 창 흐림(win:dim → backdrop blur). ③ 출력 폴더 라벨 hover 애니메이션 + 클릭 시 탐색기 열기(export:open-folder, 미존재 시 상위 폴더). ④ 진행바 sheen 을 채워진 안쪽 bar 안에서만 흐르게(overflow hidden), 가로 ×4 확대·%/메시지 상단. ⑤ 카드/폴더 정보창을 하단 파일 리스트 폭에 정렬. ⑥ ORIGINAL FILES 전체삭제[x]+확인 모달, 처리 중 원본 파일 highlight, START 버튼명 단순화·캡션 제거, 출력 파일명 (Mastered) 접두사, CONVERTED→MASTERED FILES, Clear Finished=완료 파일만 제거(작업 재개 가능). ⑦ Export 로딩 카드 원 안에 처리 단계 표시(Decoding/Denoising/Rendering/Encoding/Saving) — 단일·배치 공통. **검증:** lint·build·verify(90/90) 통과. |
| v0.9.1 | 2026-06-29 | Minor (기능) | **Render Batch — 세션 기반 일괄 Export 모달 창.** 저장된 세션(프로젝트) 설정으로 파일/폴더를 일괄 마스터링·Export. ① **모달 창**([RenderBatchWindow.tsx](src/ui/desk/RenderBatchWindow.tsx), `#renderbatch`) — `parent: mainWindow` + `modal: true` 로 메인 앱 사용 차단(요구사항 7). [main.cjs](electron/main.cjs) `win:open-render-batch`·`render-batch:get-theme`, [preload.cjs](electron/preload.cjs) `win.openRenderBatch`/`getRenderBatchTheme`. ② **세션 카드 로딩** — 상단 "Select Session Card" → `sessionIO.list/read` 로 저장된 세션을 카드로 선택, 적용 이펙트(모듈 칩·Denoise On/Off·EQ/LUFS/Format) 미니 카드로 표시. ③ **출력 폴더** — 카드 옆에 Export Destination 표시(세션 `exportDir` 또는 기본 `<Music>/Masters/<Album>`) + Change(폴더 선택). ④ **2-패널 + 세로 진행바** — 좌측 ORIGINAL FILES(+Files/+Folder/드래그드롭·삭제), 우측 CONVERTED FILES(대기 'Waiting for queue…'/처리중 스피너/완료 ✓·MASTERED 배지/오류), 가운데 세로 Progress bar(`dkbatchflow` shimmer 애니메이션·% 표시). ⑤ **렌더 파이프라인** — 신규 [batchRunner.ts](src/export/batchRunner.ts) `renderFileWithSession`: per-file 디코드→리샘플(세션 Rate)→**Denoise(토글 ON 시 곡마다 `analyzePre` STFT/SNR 분석→`getDenoiseRecommendation` 추천 depth/amount 적용 = 메인 앱과 동일 로직, 기본값 일괄 적용 아님)**→오프라인 렌더→인코딩(기존 `encodeMaster` 재사용)→`exportIO.saveFile`. LRU 없이 한 파일씩 처리·UI 양보. 우측 패널에 처리 단계(Decoding/Analyzing/Denoising/Rendering) 실시간 표시. ⑥ **Cancel** — 진행 중 CANCEL → 확인 모달("Cancel batch render?") → Yes 시 `cancelRef`·`exportCancelled` → **"Cancelling…"**(붉은 dkblink) 표시 후 현재 파일 완료 시점에 중단, 남은 파일은 cancelled 표시(이미 저장된 파일 유지). ⑦ **진입** — Project ▸ Render Batch… 메뉴([data.ts](src/desk/data.ts)·[TitleBar.tsx](src/ui/desk/TitleBar.tsx)). ⑧ **기존 Export 경로 정합** — [appStore.ts](src/store/appStore.ts) `ensureDenoiseRecommendation` 신설(`ensureDenoisedBuffer` 가 호출). Pre 패널에서 분석한 적 없는 곡(`denoiseRecommended` 미존재)은 export/재생 직전 `analyzePre`(dry SNR)→`getDenoiseRecommendation` 으로 per-file depth/amount 를 채운 뒤 denoise → **여러 곡 일반 Export 도 곡별 분석값 적용**(이전엔 미열람 곡이 기본값 2/35% 로 나가던 불일치 해소). **검증:** `npm run lint`·`npm run build` 통과. *(실제 Electron 일괄 렌더/저장·취소 동작은 사용자 시험 필요 — A3 MT-9.1.)* |
| v0.9.0 | 2026-06-29 | Minor (착수) | **세션(프로젝트) 저장/불러오기 기능 착수 — Preset형.** 마스터링 체인 설정을 세션으로 저장·복원한다. 저장 범위(사용자 확정 2026-06-29 = **체인 설정만/Preset형**): 7섹션 `vals`(단, **곡별 denoise depth/amount 제외**) + `enabled`(섹션 Bypass) + EQ 활성 프리셋 상태(`spectral.preset`·f/g/q·`activeUserPresetIdx`·`lastActivePresetName`) + Export 메타/아트워크/Destination. **II.Pre denoise**: 깊이/양(`pre.noiseDepth`/`denoiseAmt`)은 곡마다 STFT 분석 후 자동 추천되는 per-file 값이라 세션에 넣지 않고, 세션에는 `pre.denoise`(On/Off 토글)만 포함 — 적용 시 각 곡의 per-file 값·자동 추천 로직은 건드리지 않음. **창**: 별도 borderless 자식 창(`#sessions`, Preferences/About 패턴)에 적용 이펙트를 카드 형식으로 표시(Save/Load). **IO**: `userData/sessions/*.json`(user-presets 패턴). 우선 버전 상수만 v0.9.0 으로 승격(package.json·lock·version.ts), 이후 메뉴 연결→창/라우팅→IPC/IO→스토어 액션 순으로 구현. |

> **v0.8.9 이하 전체 개발 이력은 [앱개발-아카이브.md](앱개발-아카이브.md) 로 이관**(2026-06-29 1차 마무리 정리). 최근 v0.9.x 만 본 문서에 유지한다.

### v0.11.0 포함 변경 — Normalize 직접 Export 경로 수정

- [appStore.ts](src/store/appStore.ts)의 일반 Export가 `ensureProcessingBuffer()` 디코딩 전에 잡아 둔 오래된 `file` 객체를 사용해, Preview 없이 바로 Export하면 아직 측정되지 않은 `meta.peakDb` 때문에 Normalize가 적용되지 않을 수 있던 레이스를 수정했다.
- 디코딩 후 store에서 같은 ID의 최신 파일을 다시 조회하여 `previewParamsFromState()`에 전달한다. 따라서 Preview·Render Batch·일반 Export가 모두 실측 피크 기준 Normalize를 사용한다.
- 큐에서 파일이 제거된 예외 상황에는 기존 객체로 폴백한다. 태그용 메타데이터에는 영향이 없다.
- **상태:** 코드 수정·자동 검증·사용자 실앱 회귀 시험 PASS, v0.11.0에 포함.

### v0.11.0 포함 변경 — Render Batch 진행 강조 개선

- 현재 처리 중인 Batch Job 테두리에 테마 색상 glow와 부드러운 breathing 애니메이션을 추가한다.
- 처리 중인 원본/출력 파일 행을 테마 색상 배경과 테두리로 강조한다.
- Cancel 확정 즉시 현재 파일 행과 Job 테두리를 회색 비활성 상태로 전환하여 취소 진행 상태를 명확히 표시한다.
- [RenderBatchWindow.tsx](src/ui/desk/RenderBatchWindow.tsx)에서 활성 Job에 테마 glow/breathing 상태를 적용하고, 원본·출력 파일 행 모두 현재 처리 파일을 테마색으로 표시한다. Cancel 확정 시 `cancelling` 상태를 기준으로 활성 Job과 파일 행을 즉시 회색으로 바꾼다.
- [index.css](src/index.css)에 `dkbatchjobbreathe` 애니메이션을 추가했다.
- **검증:** `lint`·`build`·`verify 93/93` PASS. A3 `MT-RB-UI` 실앱 확인 항목으로 관리하며 v0.11.0에 포함.

---

# 버그

| # | 버전 | 보고일 | 내용 | 심각도 | 상태 |
|---|------|--------|------|--------|------|
| 1 | v0.1.0 | 2026-06-26 | **Electron 기동 불가(환경)**: ①사내 네트워크가 electron v33 다운로드에 깨진 바이너리를 응답, ②`ELECTRON_RUN_AS_NODE=1` 환경변수로 electron 이 일반 Node 처럼 실행되어 `app` 미초기화 크래시. | High(환경) | **해소(2026-06-26)**: 정상 설치된 electron **42.4.0** 재사용 + `scripts/run-electron.mjs` 런처(해당 env 제거)로 기동 확인. package.json/lock 42.4.0 고정. |
| 2 | v0.2.6 | 2026-06-27 | **곡 종료 시 transport 상태 미복귀**: 곡이 끝까지 재생되어도 Play 아이콘이 재생 중 상태로 멈춰 있고 위치도 리셋되지 않음. 원인: `previewEngine` 의 `source.onended` 핸들러가 `stopGraph()` 를 먼저 호출해 `graph.onEnded` 가 null 로 비워진 뒤 `graph.onEnded?.()` 를 호출 → 스토어의 `isOriginalPlaying=false` 콜백이 영영 실행되지 않음(MT-S 시험 중 사용자 보고). | Medium | **해소(v0.2.7, 2026-06-27)**: 콜백을 `stopGraph()` 전에 `notifyEnded` 로 캡처 후 호출. 곡 종료 시 정지+위치 0 리셋+Play 아이콘 정상 복귀(사용자 선택 동작). |
| 3 | v0.8.1 | 2026-06-28 | **Preview 게인 스테이징(헤드룸) — EQ 부스트 시 포화/일그러짐(클립).** 사용자 보고: I-II-III(Input→Pre→Spectral EQ)만 모니터(IV/V/VI Bypass) 시 "포화된 듯 일그러지고 소리가 상당히 커짐". 원인 = EQ 의 `+gain` 은 dB 부스트(프리셋 예: Dance +12dB·Pop +7.4dB)라 해당 대역 레벨이 0dBFS 를 넘기는데, **다운스트림 VI Loudness 리미터를 Bypass** 해 모니터 경로에 리미터가 없어 스피커 출력(±1)에서 하드클립. **Normalize(Input) ON 이면 가중** — `inputGainValue` 가 피크를 −0.1dBFS(최대 +18dB·8배)까지 끌어올려 헤드룸 0 → EQ 부스트가 즉시 클립. EQ 계산 버그가 아니라 **입력 노멀라이즈/프리셋 부스트가 다운스트림 여지를 안 남기는 게인 스테이징** 문제. *(주의: 전 섹션 ON(VI 포함) 풀체인이어도 EQ+make-up 이 Loudness 새츄레이터 헤드룸(+18dB)을 넘기면 리미터 전 클립 가능 — 별도 확인 필요.)* | Medium | **해소(v0.8.2, 2026-06-28).** 두 갈래로 정리: ① **EQ 부스트** — 사용자가 프리셋 게인을 과도하지 않게 하향(현 프리셋값 확정), 문제 없음 확인. ② **Dynamics 사용 시 "지직"** — 단계별 청취 격리로 진짜 원인이 **VI Loudness 새츄레이터**(`drive=1+a*8` base"1" 탓 기본 5%에서도 ~9% THD·피크 으깸; Dynamics make-up/압축이 신호를 그 영역으로 밀어넣는 방아쇠)임을 확정. **새츄레이터 선형↔tanh blend 재설계 + 헤드룸 ±8→±32 + 밴드 make-up 0.3→0.12 완화**로 해소(History v0.8.2 참조). 새츄레이터 도메인 ±32 확장으로 EQ+make-up 누적의 리미터 전 클립 위험도 함께 완화. |

---

# 다음 작업 할 내용 정리

> 2026-07-01 기준 후속 기능과 수정분을 v0.11.0으로 통합했다. 현재 남은 일은 Windows 설치본 확인과 향후 v1.0.0 승격 결정이다. 완료된 Phase의 계획·설계 상세는 [앱개발-아카이브.md](앱개발-아카이브.md) 참조.

## ▶ 현재 상태 요약 (2026-07-01)

| 영역 | 상태 |
|------|------|
| Phase 0~7 (셋업~Export/Batch) | ✅ 구현·시험 완료 |
| Phase 8 (테마/borderless/폴리시) | ✅ 사실상 완료(8테마·borderless·매뉴얼·Preferences/About) |
| 세션 저장/불러오기 · Render Batch (v0.9.0~9.2) | ✅ 기존 기능 구현·시험 완료 |
| 후속 기능(OGG/M4A·AIFF·9-Band·다중 Batch Job) | ✅ 구현·시험 완료, v0.11.0 반영 |
| Normalize 직접 Export 수정 | ✅ 구현·시험 완료, v0.11.0 반영 |
| 자동 시험 | ✅ lint PASS · build PASS · verify **93/93 PASS** (2026-07-01) |
| 열린 버그 | 0건 |
| **Phase 9 (자동 업데이트)** | ✅ **완료(v0.10.0)** — autoUpdater 연결·인앱 배너 구현 + v0.10.0 릴리스 발행/게시 + **M7 end-to-end PASS(2026-06-29)** |
| **Phase 10 (최종 인수 시험)** | ✅ 기존 M1~M8·M-A4/A5 + 후속 MT-10.1~10.8·MT-NORM 전부 PASS |

## ▶ 수정요청(A4) 처리 계획 — 인수시험 후속 (2026-06-30)

> [A4. 수정요청.md](A4.%20수정요청.md) 4개 항목. 사용자 결정(2026-06-30): **AIFF 전용 디코더 추가**, **항목별 개별 패치(하나씩)**. 모두 phase 변경 없는 **Patch**. ②(Release Notes 인프라) 먼저 → 이후 패치들이 거기에 고지되도록 순서 진행. 각 버전마다 Release Notes 를 **현재 버전 내용으로 교체**(이전 내용 삭제).

| 순서 | 버전 | 항목 | 내용 | 상태 |
|------|------|------|------|------|
| 1 | v0.10.1 | ② Release Notes | Help 메뉴 `Check for Updates...` 아래 `Release Notes` 추가. 새 borderless 창(`#releasenotes`, About 패턴)에 **현재 버전** 릴리스 노트 표시(버그=상세/기능·개선=간략). 내용 단일 소스 `src/releaseNotes.ts`. | ✅ 완료(v0.10.1) |
| 2 | v0.10.2 | ③ Check for Updates | `Check for Updates...` 클릭 시 `updater.check` + 결과(최신/업데이트 필요) 모달 표시(`updater:status` 구독, ExportNotice 패턴). dev 미패키징은 "설치본에서만" 안내. | ✅ 완료(v0.10.2) |
| 3 | v0.10.3 | ④ About 이메일 | [AboutWindow.tsx](src/ui/desk/AboutWindow.tsx) OK 버튼 위에 `focustone.el@gmail.com`(위·아래 한 줄 띄움). 창 높이 소폭 증가. | ✅ 완료(v0.10.3) |
| 4 | v0.10.4 | Denoise 최적화 | II.Pre-Proc Denoise 성능 최적화 (시간 도메인 탐색 + 슬라이스 STFT + L/R 병렬 Worker 처리). | ✅ 완료(v0.10.4) |
| 5 | v0.11.0 통합 | ① 결함2 OGG/M4A | OGG(Vorbis ID/granule)·M4A(MP4 atom moov/trak/mdhd/stsd) 전용 헤더 파서 추가 → 목록 최초 표시의 SR·채널·길이 정확화([decoder.ts](src/audio/decoder.ts)). MP3 폴백 가짜값 제거. | ✅ 실파일 시험 PASS·버전 반영 완료 |
| 6 | v0.11.0 통합 | ① 결함1 AIFF | AIFF 전용 PCM 디코더(SSND 파싱: 빅엔디안 PCM 8/16/24/32 + AIFF-C `sowt` 리틀엔디안 + `fl32`/`fl64` float) → 실제 재생·분석·Export 지원([decoder.ts](src/audio/decoder.ts) decodeAudioFile 경로에 AIFF 분기). | ✅ 합성 자동시험·실파일 시험 PASS·버전 반영 완료 |
| 7 | v0.11.0 통합 | III.EQ 9-Band 옵션 | 기존 Min-Phase Parametric UI/프리셋을 그대로 유지하고 `Parametric / 9-Band` 선택 추가. 9-Band는 63/125/250/500/1k/2k/4k/8k/16kHz 고정 중심 주파수와 독립 Gain을 사용하며 Preview·Export·세션·Undo에 공통 적용. | ✅ 사용자 회귀 시험 PASS·버전 반영 완료 |
| 8 | v0.11.0 통합 | Render Batch UI 개편 | **다중 Batch Job 구조로 확장.** 각 작업은 `원본 Sources → Session Card → Mastered Files/출력 폴더`를 독립 보유하며, 하단 `＋ ADD BATCH JOB`으로 작업을 추가한다. 원본은 `+ FILE`(복수 파일)·`+ FOLDER`(반복하여 복수 폴더) 및 D&D(단일/복수 파일·폴더)로 누적하고 중복을 제외한다. 상단 Start/Cancel·전체 진행률은 모든 작업의 파일을 위에서 아래로 순차 처리하며, 작업별 접기/삭제·소스/세션/출력 변경·상태 표시를 지원. | ✅ 사용자 회귀 시험 PASS·버전 반영 완료 |

- 각 항목 완료 시: History 표 기록 + [A3. 시험.md](A3.%20시험.md)에 시험항목/방법/환경 추가 + [A4. 수정요청.md](A4.%20수정요청.md)에서 해당 항목 삭제.

## ▶ Phase 9 — GitHub 자동 업데이트 · 패키징 (목표 v0.10.0)

### 9-A. autoUpdater 메인 프로세스 연결 — ✅ 완료(v0.10.0)
- [main.cjs](electron/main.cjs) `setupAutoUpdater()`: 패키징 모드에서만, 기동 직후 `checkForUpdates()` 1회. `autoDownload=false`·`autoInstallOnAppQuit=false`이며 `updater:download` 수신 시에만 다운로드한다. `checking/available/progress/downloaded/error` 이벤트 → `updater:status` IPC 로 메인 창 전달. `updater:restart`(→`quitAndInstall`)·`updater:check`·`updater:download` 수신.

### 9-B. 업데이트 UI (renderer) — ✅ 완료(v0.10.0)
- 결정: **인앱 배너 + 기동 시 1회 체크**(사용자 확정 2026-06-29).
- [preload.cjs](electron/preload.cjs) `updater.onStatus/restart/check` 노출 + [global.d.ts](src/global.d.ts) 타입. [App.tsx](src/App.tsx) `UpdateBanner` — 하단 우측 테마형 카드(다운로드 % 진행바 + "Restart now"/"Later").

### 9-C. 릴리스 워크플로 / 검증 — ✅ 완료(v0.10.0)
- 코드 서명: **미서명 시작 확정**(A1 §0). 릴리스 노트에 SmartScreen "추가 정보 → 실행" 안내 포함.
- ✅ **2026-06-29 `npm run electron:publish` 실행 성공** — GitHub `limsunglyong/FocusDAW-Mastering` 에 **v0.10.0 draft 릴리스** 생성, 에셋 3종(`...Setup-0.10.0.exe` 107.9MB · `.exe.blockmap` · **`latest.yml`**) 업로드 확인.
  - 참고: electron-builder 가 draft 를 **2개 중복 생성**(병렬 publish 현상) → blockmap-only 중복 draft 1개는 API 로 삭제하고, blockmap 을 메인 릴리스로 합쳐 단일 draft 로 정리함.
- ✅ **2026-06-29 draft 릴리스 게시 완료** + **M7 end-to-end PASS**: 시험용 구버전(0.9.9 로컬 빌드, updater 포함)을 설치·실행 → 게시된 v0.10.0 자동 감지 → 다운로드 → 인앱 배너 → 재시작 적용까지 정상 확인(사용자). **Phase 9 종료.**
- 참고(Phase 10 결정거리): NSIS `oneClick:false` 라 "Restart now" 시 설치 마법사 창이 1회 표시됨. 무인 갱신을 원하면 `oneClick:true` 검토.

## ▶ Phase 10 — 최종 인수 시험 · 릴리스 (목표 v1.0.0)

A1 §5 의 최종 Acceptance Test 를 [A3. 시험.md](A3.%20시험.md) 에 등재·수행한다. 전부 통과 시 **v1.0.0 승격(Major — 사용자 결정) 및 프로젝트 종료**.

- 자동 A1~A6: 다중포맷 디코딩 / LUFS 정합 / True Peak / Bypass 무결성 / **배치 렌더 일관성(7-H, Preview↔Export)** / 빌드 무결성.
- 수동 M1~M8와 후속 MT-10.1~10.8·MT-NORM: 모두 사용자 확인 완료.

### Phase 7 검증 결과
- 7-H Preview↔Export 정합(A5): 사용자 확인 완료.
- Batch Export 품질(M4): 현재 Batch Process 사양에는 앨범 메타/아트워크 입력 기능이 없음을 반영하여, 지원 범위 내 기능 확인 완료.

## ▶ 기술 부채 / 정리
- [src/version.ts](src/version.ts) 변경이력 주석 블록이 통째로 **2회 중복**(v0.1.x~). 기능 영향 없음. 릴리스 정리 시 1벌로 통합 권장(사용자 확정 필요).
- 빌드 경고: 메인 청크 985KB(>500KB) — 기능 영향 없음(데스크톱 앱). 필요 시 코드 스플릿.

## ▶ 승인/결정 필요 (착수 전)
- (R1) v1.0.0 Major 승격 시점/조건 — 최종 시험 전부 PASS 후.
- (R2) ✅ 후속 기능을 개별 Patch 대신 v0.11.0 통합 Minor로 반영(2026-07-01 사용자 결정).
