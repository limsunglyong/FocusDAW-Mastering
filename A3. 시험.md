# 시험(Test) 계획서

* 지침 *

- 시험은 자동 시험, 수동 시험으로 나눈다.
- 자동 시험을 할 수 있는 시험은 스스로 수행 한뒤 그 결과를 기록한다. 이후 시험에서 이전 자동 시험 항목 중 완료된 항목은 삭제 처리한다. 문제 있는 항목은 별도 섹션으로 보고서를 작성한다.
- 수동 시험 항목은 1. 수동 시험 항목 리스트, 2. 수동 시험 절차 및 판정 기준 순으로 정리한다.
- 수동 시험 항목중 완료된 것은 완료 시험 이력으로 옮긴다.
- 완료 시험 이력은 직전 버전의 수동 시험 완료 내용만 남기고 버전이 2개 이상 차이가 나면 시험-아카이브.md에 내용을 추가/누적시킨다.

## 현재 시험 항목

### v0.4.2 — 앱 아이콘 이미지 변경 (수동 시험)

[시험 환경 준비]
- `npm run dev` 실행 또는 `npm run electron:dir` 로 패키징 빌드.

[시험 항목 / 방법 및 절차]
- MT-4.2-1: `npm run dev` 로 개발 서버 실행 후 Electron 앱이 뜰 때 Windows 작업 표시줄(Taskbar)에 `logo-main2.png` 아이콘이 정상 표시되는지 확인.
- MT-4.2-2: `npm run electron:dir` 로 빌드하여 생성된 실행 파일(`FocusDAW - Mastering Desk.exe`)의 파일 아이콘 및 실행 시 작업 표시줄 아이콘이 `logo-main2.png`로 표시되는지 확인.
- (자동) AT-4.2-3: `npm run lint` 및 `npm run build` 가 에러 없이 완수되는지 확인.


## 완료 시험 이력

### v0.4.1 — 앱 아이콘 등록 (수동 시험)
- MT-4.1-1~2: logo-main.png 아이콘이 정상 등록 및 표시됨을 확인.
- AT-4.1-3: lint, build 통과.


### v0.2.28 — STFT Denoise 실처리 (수동 시험)
- MT-2A-1~5: Denoise 청감, Depth, Reduction 노브 조절, Bypass 복귀 및 3D 워터폴 연동 정상 확인 완료 (2026-06-27).
- AT-2A-6: lint, build, verify(34/34) 통과.


